<%= form_for(@dashboard,:html => { :class =>"form-horizontal"}) do |f| %>
    <% if @dashboard.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@dashboard.errors.count, "error") %> prohibited this dashboard from being saved:</h2>

          <ul>
            <% @dashboard.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="form-group">
      <label class="col-md-2 control-label"><%= f.label :title %><br></label>
      <div class="col-md-10">
        <%= f.text_field :title %>
      </div>
    </div>

    <div class="form-group">
      <div class="col-md-offset-2 col-md-10">
        <%= f.submit :class => "btn btn-primary" %>
      </div>
    </div>



<% end %>
